---
description: Bootstrap agent-materialize for this project — schema discovery, view proposals, apply, doctor.
---

Load the `setup-database` skill from `.claude/skills/agent-materialize/setup-database.md` and follow it.

If `materialize.yaml` doesn't exist yet (project not scaffolded), run `agent-mv init` first, tell the user to reconnect MCP servers (`/mcp` in Claude Code, or restart the session) so the setup MCP loads, then continue.
